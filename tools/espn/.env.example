# Dev-PC connection to the collector's Postgres on the NUC.
# Copy to tools/espn/.env (gitignored) and fill in the host + password
# (the password is POSTGRES_PASSWORD from infra/.env).
ESPN_DB_URL=postgresql://espn:CHANGE_ME@NUC_ADDRESS:5432/espn
